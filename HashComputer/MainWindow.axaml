<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
		Width="700"
		Height="280"
        x:Class="HashComputer.MainWindow"
        Title="HashComputer"
		x:CompileBindings="False"
		Icon="/icon.ico">
	<Grid RowDefinitions="Auto,*,Auto"
		  Margin="10,6,10,0">
		<StackPanel Grid.Row="0">
			<TextBlock Text="Folder path"/>

			<Grid ColumnDefinitions="*, Auto, Auto, Auto">
				<TextBox Text="{Binding FolderPath, Mode=TwoWay}"
						 TextWrapping="Wrap"
						 IsReadOnly="{Binding !IsInteractionEnabled}"/>

				<Button Content="Compute"
						Grid.Column="1"
						Margin="10,0,0,0"
						Command="{Binding ComputeHashCommand}"
						IsEnabled="{Binding IsInteractionEnabled}"/>

				<Button Content="Cancel"
						Grid.Column="2"
						Margin="10,0,0,0"
						Command="{Binding CancelCommand}"
						IsVisible="{Binding IsProgressVisible}"/>

				<TextBlock Text="Done"
						   Grid.Column="3"
						   VerticalAlignment="Center"
						   Margin="10,0,0,0"
						   Foreground="Green"
						   FontWeight="Bold"
						   IsVisible="{Binding IsDoneVisible}"/>
				<TextBlock Text="Failure"
						   Grid.Column="3"
						   VerticalAlignment="Center"
						   Margin="10,0,0,0"
						   Foreground="Red"
						   FontWeight="Bold"
						   IsVisible="{Binding IsFailureVisible}"/>
			</Grid>


			<ProgressBar Margin="0,10,0,0"
						 Minimum="0"
						 Maximum="100"
						 Value="{Binding CurrentProgress}"
						 Height="10"
						 IsVisible="{Binding IsProgressVisible}"/>
		</StackPanel>

		<TextBox IsReadOnly="True"
				 Grid.Row="1"
				 Margin="0,10,0,6"
				 Text="{Binding DiffText}"
				 TextWrapping="Wrap"
				 ScrollViewer.VerticalScrollBarVisibility="Auto"/>

		<StackPanel Grid.Row="2"
					Background="LightGray"
					Margin="0,0,0,6">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Thread 1: "
						   IsVisible="{Binding IsProgressVisible}"/>
				<TextBlock Text="{Binding CurrentFile1}"
						   IsVisible="{Binding IsProgressVisible}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Thread 2: "
						   IsVisible="{Binding IsProgressVisible}"/>
				<TextBlock Text="{Binding CurrentFile2}"
						   IsVisible="{Binding IsProgressVisible}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Thread 3: "
						   IsVisible="{Binding IsProgressVisible}"/>
				<TextBlock Text="{Binding CurrentFile3}"
						   IsVisible="{Binding IsProgressVisible}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Thread 4: "
						   IsVisible="{Binding IsProgressVisible}"/>
				<TextBlock Text="{Binding CurrentFile4}"
						   IsVisible="{Binding IsProgressVisible}"/>
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
